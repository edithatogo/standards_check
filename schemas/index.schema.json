{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://example.org/schemas/index.schema.json",
  "title": "Checklist Index",
  "type": "object",
  "required": ["version", "updated", "items"],
  "properties": {
    "version": { "type": ["string","number"] },
    "updated": { "type": "string", "pattern": "^\\d{4}-\\d{2}-\\d{2}$" },
    "items": {
      "type": "array",
      "items": {
        "type": "object",
        "required": [
          "id", "title", "url", "version", "date", "license",
          "publisher", "preferred_format", "level", "group"
        ],
        "properties": {
          "id": { "type": "string", "pattern": "^[a-z0-9]+(-[a-z0-9]+)*$" },
          "title": { "type": "string" },
          "url": { "type": "string", "not": { "const": "TBD" } },
          "version": { "type": ["string","number"], "not": { "const": "TBD" } },
          "date": { "type": ["string","null"], "not": { "const": "TBD" } },
          "license": { "type": "string", "not": { "const": "TBD" } },
          "publisher": { "type": "string", "not": { "const": "TBD" } },
          "preferred_format": { "type": "string", "enum": ["pdf","html","docx","csv"] },
          "level": { "type": "string", "enum": ["archetype","variant"] },
          "group": { "type": "string" },
          "variant_of": { "type": "string" },
          "status": { "type": "string" },
          "notes": { "type": "string" }
        },
        "additionalProperties": true
      }
    }
  },
  "additionalProperties": true
}

